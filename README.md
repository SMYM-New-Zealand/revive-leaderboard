# Instructions

First clone the repository. Once inside the project directory, run `npm install` to install the dependencies. Afterwards run `npm run dev` to launch the app. 

To deploy the app to GitHub Pages, first run `npm run build` then `npm run deploy`. 
